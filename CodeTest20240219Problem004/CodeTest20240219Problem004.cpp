// CodeTest20240219Problem004.cpp : 이 파일에는 'main' 함수가 포함됩니다. 거기서 프로그램 실행이 시작되고 종료됩니다.
//

#include <iostream>
#include <vector>
using namespace std;
// 전부 0으로 초기화
// 좌표에 대한 
int main()
{
	int room;
	cin >> room;
	vector<vector<int>> rooms(room+1,vector<int>(room+1,0));//0불꺼짐
	int board;
	cin >> board;
	for (int i = 0; i < board; ++i)
	{

	}
}
